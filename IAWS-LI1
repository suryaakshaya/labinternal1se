Exp 1:(Connect EC2 Ubuntu Using PuTTY (.ppk Key))
1. EC2 instance (.ppk file)
2. Launch putty then on ec2 instance click connect there you will find public ipv4 copy and in putty fill as ubunut@10.11.x.x
3.Connection → SSH → Auth → Credentials->browse->select ppk file ->open
4.click yes then to verify click whoami(ans:ubuntu)
Using .pem to convert to .ppk so now create instance with .pem
1.open puttygen click on load and in thee dropdown select allfiles then select the pem file click save private key and save it. Now follow the same steps putty and so on.
Connecting to EC2 Using AWS CloudShell:
1. Cloudshell upload your .pem click ls to verify now write command chmod 400 filename then go to instance->connect->ssh client copy command ssh-i paste and verify with whoami (ans:ubuntu)


Exp 2A:(1.	a) Create and attach an EBS volume to an EC2 instance, and b)	scale the instance by increasing CPU and RAM by changing the instance type.)
1.EC2 instance (.ppk) -> volumes create volume in same az as instance->select volume->actions->attach volume -> select ec2 instance and device name connect.
2. Verify by connecting to ec2 instance->connect->connect command: lsblk
3. Increasing or decreasing capacity: ec2 dashboard->stop instance->actions->instance settings->modify volume->modify->click modify-> start again now you will see the change,
Exp 2B:(2.Attach and permanently mount an EBS volume to a Linux EC2 instance to ensure data persistence across reboots.)
sudo fdisk /dev/nvme1n1->n->p-><Enter>-><Enter>-><Enter>->w->sudo partprobe->lsblk->sudo mkfs.xfs /dev/nvme1n1p1->sudo mkdir /mnt/archana->sudo mount /dev/nvme1n1p1 /mnt/archana->df -h->sudo blkid /dev/nvme1n1p1(get UUID)
->sudo nano /etc/fstab in this file paste this line UUID=YOUR_UUID_HERE   /mnt/archana   xfs   defaults,nofail   0   0(ctrl+o->enter->ctrl+x)->sudo mount -a->cd /mnt/archana->sudo chown ubuntu:ubuntu /mnt/surya->touch f1->ls->sudo reboot-> df -h->ls /mnt/surya
Exp 2C:(3. Create a snapshot of the attached EBS volume and use it to create and attach a new volume to an EC2 instance in another AWS region.)



Exp 3:EFS
1.two ec2 instances one in a and another in 1b sg(ssh-22-anywhere,nfs-anywhere)
2.create efs-vpc default->enable region->add the newly created sg
3.open two powershells and connect using ssh then follow these commands in both:
sudo su->apt update->apt install -y nfs-common->mkdir /efs->mount -t nfs4 -o nfsvers=4.1 fs-XXXXXXXX.efs.us-east-1.amazonaws.com:/ /efs(replace with dns in efs)->df -h
4.Testing: cd /efs->echo "hello" >ex.txt->ls in another cd /efs->ls


Exp 4: S3
create s3 bucket->disable block public access->enable acls->open bucket->permissions->acl edit->public(anywhere) read tick
upload object->click on object->acls->read->open url if access denied paste this in bucket->permissions->bucket policy->
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "PublicReadGetObject",
      "Effect": "Allow",
      "Principal": "*",
      "Action": "s3:GetObject",
      "Resource": "arn:aws:s3:::YOUR_BUCKET_NAME/*"
    }
  ]
}replace with nucket name
For versioning first diable versioning then enable and upload then it works
CRR: create another bucket in oregon enable versioning->src bucket->management->repliaction rules->create rep rule
->status:enabled->scope->all objects->labrole->pop up->no->submit->upload in src->Wait for one min->you will see in dest



Exp 5:VPC creation and EC2 connection
vpc->create->vpc only->10.0.0.0/16->public subnet->private subent
craete igw->attach vpc->create route table->select vpc->Add route->dest=10.0.0.0/16 type=igw
select table->subnet ass tab->edit subnet ass-> tick the public subnet->save
private route table->everything default->save
public subnet->edit subnet ass->enable auto assign public ipv4
create sg->2.	Add inbound rules:HTTP (80) / HTTPS (443)->SSH (22) from trusted IP
attach to both public and private instances
public instance :
network settings ->edit->select vpc->publci subnt->Auto assign->enable->select sg
private instance: vpc,sg->Same->private subnet->auto assign->disable
verify: public can be accessed through ssh  in cmd but not priavte insatnce



Exp 6:VPC using NAT
create vpc->10.0.0.0/16
public subnet->10.0.1.0/24->routetable->0.0.0.0/0(igw)
private subnet->10.0.2.0/24
internet gateway->attach vpc
sg->ssh(bastion)myip
sg->pr->ssh(bastion pri ip)->mysql=10.0.1.0/24
network acl->attach one to both:100=22;110=3306;120=1024-65535
create ec2instances:bastion(ENABLE);db(DISABLE)
nat gateway->publicsn->Alocate automatically
private rt->0.0.0.0/0->nat gatewayt
ssh -i "C:\Users\info\Downloads\bastion.pem" ubuntu@<BASTION_PUBLIC_IP>
scp -i "C:\Users\info\Downloads\bastion.pem" "C:\Users\info\Downloads\db.pem" ubuntu@<BASTION_PUBLIC_IP>:/home/ubuntu/
ls
chmod 400 db.pem
ssh -i db.pem ubuntu@<DB_PRIVATE_IP>
sudo apt update
sudo apt install mysql-server -y
sudo systemctl start mysql
sudo systemctl status mysql


Exp7:Dynamodb and lambda
create a bucket in s3(disable and check only,versioning also disabled)->go to dynamo->create table->partition key->unique(string)->go to lambda->create func->author->python 3.10->craete.
go to the function->configuration->permission->clickable role link->add permissions->attach policies->amazons3fullaccess and amazondynamodbfullaccess->done
go to lambda->code ->replace this code:
import boto3
from uuid import uuid4

def lambda_handler(event, context):

    dynamodb = boto3.resource('dynamodb')
    dynamoTable = dynamodb.Table('newtable2')

    if 'Records' in event:
        for record in event['Records']:

            bucket_name = record['s3']['bucket']['name']
            object_key = record['s3']['object']['key']
            size = record['s3']['object'].get('size', -1)
            event_name = record['eventName']
            event_time = record['eventTime']

            dynamoTable.put_item(
                Item={
                    'unique': str(uuid4()),
                    'Bucket': bucket_name,
                    'Object': object_key,
                    'Size': size,
                    'Event': event_name,
                    'EventTime': event_time
                }
            )

    return {
        "statusCode": 200,
        "body": "Data stored in DynamoDB"
    }
deploy
now click create trgger->Src->s3->bucket name->now add item in bucket it will be visible in table.



Week-8:
Same like above bucket->sns->create topic->standard->create sqs->standard->create
open sns->create subscription->end point->sqs->Select the queue
go to s3->properties->add event notification->all events creation->sns topic->select sns->create
upload file in s3->now go to sqs->send and recieve messages->poll messages rows will appear.
in sns policy just change to arnEquals in sqs update the access policy.
{
  "Version": "2008-10-17",
  "Id": "__default_policy_ID",
  "Statement": [
    {
      "Sid": "__default_statement_ID",
      "Effect": "Allow",
      "Principal": {
        "AWS": "*"
      },
      "Action": [
        "SNS:GetTopicAttributes",
        "SNS:SetTopicAttributes",
        "SNS:AddPermission",
        "SNS:RemovePermission",
        "SNS:DeleteTopic",
        "SNS:Subscribe",
        "SNS:ListSubscriptionsByTopic",
        "SNS:Publish"
      ],
      "Resource": "arn:aws:sns:us-east-1:304896042643:mytopic1",
      "Condition": {
        "StringEquals": {
          "AWS:SourceOwner": "304896042643"
        }
      }
    },
    {
      "Sid": "AllowS3Publish",
      "Effect": "Allow",
      "Principal": {
        "Service": "s3.amazonaws.com"
      },
      "Action": "SNS:Publish",
      "Resource": "arn:aws:sns:us-east-1:304896042643:mytopic1",
      "Condition": {
        "ArnLike": {
          "aws:SourceArn": "arn:aws:s3:::YOUR-BUCKET-NAME"
        },
        "StringEquals": {
          "aws:SourceAccount": "304896042643"
        }
      }
    }
  ]
}



Week 9:
create a instance->ssh and http anywhere connect and run the following commabds:
sudo yum update -y
sudo yum install httpd -y
sudo systemctl start httpd
sudo systemctl enable httpd
echo "This is server 1"|sudo tee /var/www/html/index/html
now create second instance and do the same 
create sg for lb http anywhere
create target grp select the both ec2 instances and register them
now create lb application from ec2 dashboard select sg and target grp def vpc wait until status is active now go to target grp and check 2 healthy will be there 
now open dns you will get 1 and refresh you will get 2
select  a instance->action->image->create ami
ec2->launch templates->select ami->http anywhere->create
create auto scaling grp->select template->vpc->all subnets->existing lb->desired 2->min1->max4
now wiat till active->terminate one instance ->it will be created automatically watit and watch



Week10:
Go to elastic beanstalk and create app tomcat->web env->lab role->elasticbeanstack edit in the policy:"elasticbeanstalk.amazonaws.com"
to create labrole: elastic beanstalk ani click chesi webtier,workertier,multicontainerdocker,managedupdatescustomerrolepolicy add chey
to create lab instance profile: webtier,instance core
service role:labrole, instanceprofile:labinstanceprofile
vpc-def,subnets select all,enable auto assign public ip,create 
if fails go to lab role->policy makee sure in the code elasticbeanstalk.amazonaws.com is there.




Week 11:
Go to amazn lex:
create bot->create new iamrole->intents->add intent->name->slots as required->click on advanched options then conditional branching(slot prompts->more promt options->add card)
slot types->custom
inital message confirmation response->build->test





Week12:
Go to iam->create role->enter name->provide access to aws management console->pass->permissions->s3 ull acess->Download csv file
logout sign in with link given in csv file 
Test:ec2 should fail and s3 should work
Creating access keys:
login as root->users->ur role->security cred->create access key->cli->download csv
now go to browser and download aws cli->enter the values using the csv file and test ec2 will fail by 
aws configure
aws ec2 describe-instances
aws s3 ls
aws s3 mb s3://new-name 
verify in buckets again.


Week13:
Go to iam->users->create role->Aws service->ec2->add policy->s3full access->create ec2 instance->select->Actions->Security->modify iam role->newly created role->connect to ec2 thrpugh ssh and type command aws s3 ls
it should work and aws ec2 describe-instances should fail
